{
  "name": "agent-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AG-UI + Claude Agent SDK server. Compose skills (MCP tools + system-prompt fragments + bash whitelist + required env) into one HTTP/SSE endpoint.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "agent-core": "workspace:*",
    "@ag-ui/claude-agent-sdk": "^0.0.2",
    "@ag-ui/core": "^0.0.53",
    "@ag-ui/encoder": "^0.0.53",
    "@anthropic-ai/claude-agent-sdk": "^0.2.138"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.5.0",
    "vitest": "^4.1.5"
  }
}
